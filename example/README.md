Example of grapevine messaging in action.

Usage:
1. `$ npm install` (auto-does bower install)
2. `$ npm start`
3. open `localhost:3000` any number of times you like.
4. open `localhost:3000/sendMessage` (see message changed)
